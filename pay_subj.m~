function pay_subj(subID)
%% pay_subj('999-1')

%flip a coin to determine whether trial is from BDM or choice trials
p = rand;
if p > 0.5
    %BDM
    pay_subj_BDM(sub
else
    %CHOICE
    trial = 1;
end



end